def unsafe?(speed)
  if speed == unsafe
    return true
  else 
    return false

end



def not_safe?(speed)
	speed = 40<> 60 == true
	speed 30 ? "safe" : "unsafe"
	speed 95 ? "safe" : "unsafe"
	speed 95 ? "safe" : "unsafe"
	return speed
end
	


